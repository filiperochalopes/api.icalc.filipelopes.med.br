services:
  drcalc-api:
    build:
      context: .
      target: development
    command: sh -c "yarn prisma:generate && yarn dev"
    volumes:
      - .:/app
      - drcalc-api-node-modules:/app/node_modules
    ports:
      - "${APP_PORT:-8086}:4000"
    environment:
      DATABASE_URL: mysql://${DB_USER:-root}:${DB_PASS:-root}@drcalc-api-db:3306/${DB_NAME:-drcalc}
    depends_on:
      drcalc-api-db:
        condition: service_healthy
    networks:
      - drcalc-dev

  drcalc-api-db:
    image: mariadb:11.4
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASS:-root}
      MYSQL_DATABASE: ${DB_NAME:-drcalc}
    volumes:
      - drcalc-api-db-data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mariadb-admin ping -h localhost -uroot -p\"$$MYSQL_ROOT_PASSWORD\" --silent"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - drcalc-dev

  drcalc-api-studio:
    build:
      context: .
      target: development
    command: sh -c "yarn prisma:generate && yarn prisma studio --browser none"
    volumes:
      - .:/app
      - drcalc-api-node-modules:/app/node_modules
    ports:
      - "${PRISMA_STUDIO_PORT:-5555}:5555"
    environment:
      DATABASE_URL: mysql://${DB_USER:-root}:${DB_PASS:-root}@drcalc-api-db:3306/${DB_NAME:-drcalc}
    depends_on:
      drcalc-api-db:
        condition: service_healthy
    networks:
      - drcalc-dev

volumes:
  drcalc-api-node-modules:
  drcalc-api-db-data:

networks:
  drcalc-dev:
    driver: bridge
